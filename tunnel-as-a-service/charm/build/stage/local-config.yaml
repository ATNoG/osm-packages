# this parameters should be adapted to
# the charateristics of your destination VM
prometheus-node-exporter:
        ssh-hostname: 10.0.12.95
        ssh-username: ubuntu
        ssh-password: password
